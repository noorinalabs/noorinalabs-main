# Promotion Audit — wave-25 (2026-07-19)

Deterministic driver: `promotion-audit/run.py wave-25`.

**Result: 0 AUTO · 0 DECIDE · 207 KEPT · 24 SUPERSEDED.**

Nothing crossed a promotion threshold this wave. The 24 SUPERSEDED entries are memories already codified via `Promotion provenance:` blocks in `charter/hooks.md` (or enforced-elsewhere in charter), plus 3 skills already enforced via registered hooks (board-audit, promotion-audit, wave-wrapup). No AUTO promotions to open, no DECIDE (skill→hook) issues to file.

Note: the one genuine skill→hook-class candidate this wave — hardening Hook 4 to fail CLOSED on an unresolvable `-R`/`--repo` — is already filed as main#981 (not surfaced by the audit because the signal is a filed bug, not a promotion-threshold crossing).
