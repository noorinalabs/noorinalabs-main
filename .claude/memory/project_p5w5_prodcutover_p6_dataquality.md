---
name: project_p5w5_prodcutover_p6_dataquality
description: "Owner direction 2026-06-16 — P5W5 = production cutover + landing polish + heavy TD. ROADMAP REVISED 2026-06-19: insert NEW Phase 6 = Claude Efficiency; the Data-Quality/ML phase advances to Phase 7. Plus standing policy: last wave of a phase relaxes the +20% TD-intake cap."
metadata:
  node_type: memory
  type: project
  originSessionId: b923c0f4-c87a-4bed-b4b8-91a79287509b
---

**UPDATE 2026-06-20 — P5 CLOSED, P6 PLANNED & LIVE.** Full wave-close + phase-transition executed this session: `/wave-wrapup` (45 PRs / 0 CR / 9% concentration / staging+fan-in green / 11 worktrees cleaned / #692 closed), `/wave-retro` (PR #724 merged, 2 approvals; promotion-audit 0/0/248/19; annunaki 47=40 exit-0 FPs+5 blocks+2 events, no actionable class), `/phase-review 5` (4 core trackers **#665/#602/#603/#666 CLOSED** on the **cutover bar**; #603 fully met; queryability/search/data-quality honestly carried to **P7 #723**; #667 streaming→P7; phase-5.md `status: exited`). `/plan-phase 6` created **phase-6.md** + trackers **#725** (memory leanness), **#726** (code-over-prose), **#727** (persona spike-and-decide), **#728** (ontology-vs-graphify spike-and-decide), **#729** (annunaki exit-0 precision), **#730** (wrap-on-last-merge), W1 meta **#731**; 8 framework-TD issues re-homed to `phase-6`; **15 stale `phase-6` (old data-quality) issues relabeled → `phase-7`**. `cross-repo-status.json`: `current_phase=6`, `phase_6_active=true`, `phase_6_wave_1_meta_issue=#731`. Persona + ontology revisits confirmed **spike-and-decide, not pre-commit** (owner 2026-06-20). Next: `/wave-scope 6 1` → `/wave-kickoff` for P6W1.

---

**Owner direction (2026-06-16) for Phase 5 Wave 5 and beyond:**

- **P5W5 theme = Production cutover** — get real data live on **prod** (P5 end-state criterion #4 / main#666). **UPDATE 2026-06-18: prod Neo4j corpus load DONE + verified** — prod went from 0 nodes to the full verified corpus (Hadith 768,920 · Narrator 132,999 · Grading 69,465 · Collection 48; matches staging to the digit), via a fresh OFFLINE neo4j-only `neo4j-admin database dump`(staging)→`database load --overwrite-destination`(prod) — NOT `restore.sh latest` (its `restore_postgres()` would `pg_restore --clean` prod PG). Prod PG untouched. Owner sequenced this as a discrete data-only step.
- **UPDATE 2026-06-19 (cutover WRAPPED just before a session crash; re-verified next session):** the app-stack roll completed. Sequence on 06-19: promote (image retag stg→prod) at 19:26 retagged all 4 prod images (api/frontend/user-service/landing) SUCCESS but its auto-dispatch substep "Trigger prod VPS rollout" FAILED; a standalone **`deploy-prod` ran at 19:40 and SUCCEEDED**, rolling the prod stack (adds the `isnad-graph-embed` service). Prod is LIVE: frontend serves, **`/api/v1/collections` returns `401` (route live, auth-gated as designed)**. **REMAINING LOOSE END: prod corpus re-embed (semantic search) NOT finished** — the `Re-embed corpus` run was **CANCELLED at 19:47** (right as the session crashed, run 06-18 17:31 also non-success); re-run `reembed-corpus.yml` against prod to complete it. Close-out (deploy#470, main#692/#666, prod-dependent exit criteria #665/#602/#603, and #667 streaming-repeatable assessment) was interrupted by the crash and is still OPEN pending verify+close. Meta-issue **noorinalabs-main#692**. Originally-planned W5 ("Historical timeline & events", #673) slid to **Phase 7 or later** (see below).
- **Pull in landing-page tickets** — pre-launch polish; owner observed the landing page still shows **fake/placeholder staff members** (filed **lp#138**); sweep for other placeholder/fake content while there.
- **Heavy tech-debt, relaxed ratio** — see standing policy below; deliberately pull in a large chunk of TD (well beyond +20%) this wave.
- **Cross-repo open-issue census first** — scan EVERY open issue across all 8 repos (incl. issues NOT on project board 2 — off-board detection via `comm -23 <all-open-urls> <board-urls>`), verify each (real / makes-sense / already-solved / phantom), and disposition (W5 / FUTURE-P6 / close) to choose W5 scope. Run before `/wave-scope 5 5`.

**STANDING POLICY (owner 2026-06-16): the last wave of a phase relaxes the +20% TD-intake cap.** On a phase's final wave the +20% per-wave tech-debt intake (`/wave-scope` Step 8.5, memory [[feedback_td_intake_20pct_per_wave]]) becomes a **floor, not a cap** — deliberately clear a large chunk of debt before phase exit; the crowd-out cap does not apply. Being codified to charter + wave-scope Step 8.5 (Aino, PR pending) — that charter section is the durable record once merged.

**ROADMAP REVISION (owner 2026-06-19) — a phase is inserted and everything downstream advances +1:**

- **Phase 6 (NEW) = Claude Efficiency.** Largely audit + interaction-based; tighten how the agentic framework itself works. Workstreams: (1) **tighten memories** (this MEMORY.md/topic-file curation is a first cut); (2) **bias toward CODE over `.md`/memories** — prefer executable artifacts to prose docs and memory entries; (3) **revisit the persona model** — owner instinct is that agent *attributes* don't do much and feedback trends to all-agents-perfect-score, i.e. we may be over-modelling human-org reality for processes that don't carry their weight in an agentic framework; (4) **explore the new Google document format/structure for LLMs + "graphify"** in service of **dropping our current ontology setup** (the tracker-hook/resolver-skill/librarian-skill + Hook 15 stack, [[project_ontology_system]]); (5) **tech debt + Claude updates/fixes** — including TD/fixes **not tied to a phase** — are explicitly in-scope for P6. Framing is **investigation-first** for the persona + ontology items (owner language: "revisit", "explore", "I think maybe") — audit → recommend → owner decides, not pre-decided removal. Touches the [[project_bootstrap_repo]] dogfood pattern.
- **Phase 7 = Data Quality / ML / demo** (the phase formerly slated as P6). Serious ML analysis + pipeline work to make the data **usable and fit to show in a demo**: deep NER/entity-resolution accuracy, dedup, segmentation depth (beyond da#158), embedding quality at scale, a real model served **in prod** (vs MiniLM proven on staging — see isnad-graph's semantic-search memory), validation/integrity gates. data-acquisition + ingest-platform likely its center. Historical timeline & events (#673) lands here-or-later too.
- Formal plan docs are created at **`/plan-phase 6`** (then `/plan-phase 7`) per the phase-5 exit gate — not pre-written now; this memory + the phase-5.md deferred-section pointer are the lean stage-setting.

**Cross-phase key hazard recurred:** `wave_5_scope` carried the stale **P4W5** value ("Phase-4 exit drive") into P5 — the `wave_{M}_*` cross-phase collision ([[project_wave_key_cross_phase_collision]], main#683). Reset manually at theme-set. Same for `wave_5_meta_issue` (was stale int 645 = a P4W5 issue). The deterministic-key fix remains the durable answer.
