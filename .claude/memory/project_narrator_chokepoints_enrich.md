---
name: project_narrator_chokepoints_enrich
description: main#928 from-parse artifact (2026-07-12-bd133e6, 107,951 canonical) LOADED+PRUNED+ENRICHED on stg at parity; prod HELD (backup-gated). Two [!-~] locale bugs fixed to unblock the prune (deploy#600/#602).
metadata:
  type: project
---

> ## 2026-07-13 — PROD PROMOTION FILED as a dedicated **Prod Cutover** phase (owner: "warrant issues, next phase"). NOT started; backup-gated.
>
> stg flag-now is DONE+verified (below). Prod is NOT ready and NOT a small op: prod still runs the pre-cutover graph (~209,944 nodes, pre-da#356 chimeric election); the from-parse reload/prune/enrich was **stg-only**. Owner chose (AskUserQuestion): **explicit backup gate** + **dedicated prod-cutover phase** (kept out of feature-wave labels → represented as a `Prod Cutover` **milestone #1** in main + deploy, NOT a wave label).
> - **Meta: main#959** (Prod Cutover phase tracker; links #928/#958/#916 + backup scaffolding deploy#582/#568/#586/#592/#593/#596).
> - **deploy#609** — backup go/no-go sign-off GATE (real backup executed BOTH hosts incl user-postgres/audit_log + B2-verified artifact + restore rehearsed GREEN-with-content-assertions, shown RED-against-corruption first). HARD precondition. NOTE: deploy#558/#559/#560 closed = mechanism merged, NOT "a backup has run / restore proven" — #609 exists precisely to make the executed go/no-go explicit. Related open hardening: deploy#582 (restore-rehearsal from real B2 object + freshness probe), #568/#586/#592/#593/#596.
> - **deploy#610** — prod graph cutover (reload→prune→enrich the corrected from-parse artifact at stg parity). Blocked by #609.
> - **deploy#611** — extend `over_merged` flag op to `env=prod` behind the `production` double-gate (deploy#608 hard-refuses prod today). Blocked by #609 + #610.
> All 4 on project board 2; deploy issues carry back-link comments to #959. **Nothing dispatched. Prod stays HELD; each step needs explicit owner go-ahead; prod prune NOT MVP-waived.**

> ## 2026-07-12 — PR-B SPLIT ABANDONED (spike NO/NO, proven on the shipping engine); owner pivot = FLAG-NOW (#337) + external-evidence split later (#443).
>
> The da#337/#346 over-merge is **real but unsplittable with corpus-internal evidence.** A read-only feasibility spike (Jean-Claude, artifact `bd133e6`) ran the isnad-neighbour context discriminator at **Gate-1-single** — the only gate where the contaminants live (Gate-2/3 are EMPTY: 0 of 1,434 candidates, so PR-B as-specced is **inert** in prod). Verdict **NO / NO**, then **confirmed + sharpened by running Kavitha's ACTUAL engine (branch `K.Sundaramurthy/0337-context-discriminator` 38236a4) on real data**:
> - **Chimera bare ʿAbd Allāh: does NOT clean.** 51 qualifying communities > `SPLIT_MAX_PEEL=50` → abstains (ctx_noise); largest community is a ~30% mega-blob spanning attested dates **−44 → 805 AH** (~850 yr), welded because the hadith collectors' shared late teachers (Yazīd b. Hārūn, Ibn Abī Shayba, Suwayd b. Naṣr) co-occur across many distinct ʿAbd Allāhs. Intrinsic to isnad structure, not tuning.
> - **Genuine hub al-Zuhrī: actively CORRUPTED.** 6 qualifying communities → guard fires → peels **5 fabricated splinters with impossible dates** (`ctx:ثابت الزرقي` mid=594 AH for a man who died 124 AH; mid=0; mid=203). False-splits the single most important tābiʿī transmitter.
> - **THE PROCESS FINDING (load-bearing):** Kavitha's **synthetic** al-Zuhrī fixture (one clean giant community → stays whole) **PASSED** — but **real** al-Zuhrī FAILS, because a real hub has small pockets repeatedly sharing one rare neighbour (21 mentions all flanked by ثابت الزرقي) that form ≥10-support Jaccard-0 communities tripping the ≥2 condition. The toy fixture was too clean to contain them → false green light. Textbook [[feedback_silent_zero_is_not_a_measurement]] + [[feedback_fixture_makes_guard_assertion_inert]]: **verify the instrument on real data of BOTH classes, never a synthetic fixture.**
>
> Kavitha's engine is clean, well-built code — the DATA doesn't carry the signal. Her **#411** (composite `(hadith_id, chain_index)` chain-index) + the `plan_split` abstain_reason refactor stand on their own; branch retained (no PR) for reference + as reusable flag-detector inputs.
>
> **Owner decision (AskUserQuestion, 2026-07-12): FLAG-NOW + external-evidence split later; then AskUserQuestion #3 = SEED NOW, GROW LATER.**
> - **Flag-now — CRITICAL detector negative result (Jean-Claude, measured on bd133e6):** there is **NO clean corpus-internal threshold.** The proposed date-span signal FAILS its bidirectional control — chimera ʿAbd Allāh span 528 AH, but genuine hubs Shuʿba 599 / Qatāda 609 / Mālik 530 / Ibn ʿAbbās 531 span AS WIDE OR WIDER (structural: every heavy transmitter chains down to the late collectors; the −44→805 AH "impossible span" is UNIVERSAL attested-death pollution, not a chimera marker → filed da#446, NOT the flag signal). Neighbour-diffuseness separates better (hubs ≤0.15, chimeras ≥0.31) but MISSES bare Sufyān (0.15, a 2–3-person fusion each keeping a tight circle). So the flag is **NOT an automated threshold** — it is a **high-recall automated QUEUE** (`is_generic_name` incl. Sufyān + top-N betweenness + diffuseness≥0.25 as *priority*, never date-span) → **external/rijāl CONFIRMATION** → a small **curated `over_merged` list**. Pure boolean annotation (`over_merged: bool` + `over_merge_note: str`), **zero minted nodes**; set by a resolve stage (rides re-run) + an immediate graph-ops cypher SET (ships to stg WITHOUT a reload). Bidirectional acceptance enforced by a committed fixture: MUST-EXCLUDE al-Zuhrī/Abū Hurayra/Mālik/Ibn ʿAbbās/Shuʿba/Qatāda/al-Aʿmash/Nāfiʿ/Maʿmar (flagging any → CI fail); MUST-INCLUDE the seeded chimeras.
> - **Cross-repo issues filed + on board:** meta **main#958**; producer **da#445 MERGED** (PR#447 merge 5d0958c3, 2/2 Approved — Nikolaos verified the bidirectional fixture is genuinely fail-closed incl+excl paired on same set; Ivana ruled أبو بكر IN, bare-kunya `nar:cf44624b…` is a DISTINCT node from Companion al-Ṣiddīq `nar:f8359bd7…`; contract `over_merged: bool` + `over_merge_note: str` now on da main schemas.py:124-125; 8-name seed yaml on main. **Deliverable-2 correction (verified at src): `is_generic_name("سفيان",26876)` ALREADY True — no `generic_name.py` change; the real Sufyān escape is `narrator_split`'s `... AND NOT is_registered_mononym` (Sufyān ∈ MONONYM_REGISTRY da#248) = a da#443 QUEUE-predicate concern, recorded there. Pinned trio: bare ism True / 2-token ism+nisba `سفيان الثوري` True BY DESIGN / ≥3-token ism+nasab False**); consumer **ig#1183** (choke-point UI reads `Narrator.over_merged`, annotate-and-separate, does NOT recompute betweenness); transport **deploy#603** (graph-load carries the property + live-graph cypher SET). ig#1183 + deploy#603 fan out once da#445 locks the property + curated list. **Seed = provably-definitional bare generics only** (bare ʿAbd Allāh, bare Sufyān + by-construction over-merges); Companion names (bare Abū Bakr) need the Arabic/rijāl reviewer lens or stay out. Full spec on **#337** (flag-now comment).
> - **External split (filed #443):** the *real* split waits for an external rijāl authority carrying distinct person-ids (nasab + ṭabaqa + teacher/student edges) — the disambiguating signal the corpus lacks. Acceptance gate baked in: **real-hub A/B with ZERO peels required, NOT synthetic fixtures.**
>
> **Wave state:** MERGED (6) — PR-A #437 (#411), #435 (#382), #436 (#364 comments), #441 (#397/#398 counters), PR-E #440 (#434 fold+matcher), **#442 (#370 attestation, merge b6428f36)** — Nikolaos caught that `narrator_split` is a 4th writer of `narrators_canonical.parquet` mutating `mention_count` after the last derive site (peeled-to-0 primary stayed `isnad_attested`, peeled bands loaded `unknown`); fix = narrator_split as 4th `derive_attestation` site + biting test, 2/2 Approved at c1ce2f25. Filed **#444** (Gate-3 dead code: `SPLIT_BAND_GAP=80 > SPLIT_MIN_SEPARATION=50` unreachable). DEFERRED behind bidirectional-unweighted A/B + Arabic rijāl: #347b, #431, #397/#398 behavioral loosen, #364 mis-crosswalk. **Re-run batch parked** behind the flag work (producer da#445 → transport/consumer); headline is "honest leaderboard via flagging + safe upstream fixes," not "split." **Prod HELD** (backup-gated).
>
> **FAN-OUT (2026-07-13, after da#445 merged):** contract locked → both downstream legs commissioned in child worktrees. **ig#1183 consumer = Mateo Salazar** (branch `M.Salazar/1183-annotate-over-merged`, wt d7b2307): choke-point endpoint returns `over_merged`+`over_merge_note` per node, React annotate-and-SEPARATE (badge + separate section), does NOT recompute betweenness. **deploy#603 transport = Aisha Idrissi** (branch `A.Idrissi/0603-over-merged-transport`, wt 5c1a0e8): (1) durable graph-load carries the property into Neo4j; (2) stg-ONLY live-graph cypher `MATCH (n:Narrator) WHERE n.id IN $ids SET n.over_merged=true` mirroring the fawaz/prune graph-ops workflow (cypher-shell 5.x: no `:auto`, params not interpolation). **$ids = producer's authoritative 8-id resolution (requested from Kavitha; do NOT re-derive deploy-side — da#371/#376 canonical-id drift).** Both: PR to main, 2 child-roster reviewers, no self-merge. **stg dispatch is a FOLLOW-ON the orchestrator runs after deploy#603 merges + ids confirmed — NOT the implementer. PROD dispatch NOT built, NOT authorized (backup-gated).**
>
> **PROGRESS 2026-07-13:** Producer DONE — **da#447 MERGED** (5d0958c3, flag mechanism) + **da#448 MERGED** (1cd414ec, stale Abū Bakr note cleanup — the note is user-facing tooltip text). Consumer **ig#1184 MERGED** (596ecdbe, #1183 closed): endpoint returns `over_merged`+`over_merge_note`, React badge+explainer on narrator-detail + graph-explorer panels, absent-property coerced→false (tested on a NO-property node, not just false — matters because live stg sets the prop on only 8 nodes), betweenness disclosed-in-place never dropped, 7-locale i18n incl Arabic RTL. Anya+Ravi approved w/ real verification (verified the "no ranked leaderboard exists → badge IS the disclosure" claim). Follow-up **ig#1185** filed+boarded (per-hadith chain endpoint doesn't fetch over_merged → flagged nodes look unflagged there; out-of-scope coverage gap). **DEPLOY DESIGN REVISED (orchestrator call): the live stg cypher sets `over_merged=true` ONLY — NOT `over_merge_note`.** Why: (1) the seed note text contains `"`/`>`/`%` which the injection-safe shell charset-whitelist correctly rejects — free text must NOT be shell-interpolated into cypher; the typed **parquet reload** is the safe path for notes; (2) a single shared `$note` on all 8 (Aisha's first cut) = wrong data (each note distinct) + diverges from reload; (3) disclosure = badge + i18n explainer, NOT the note (note is supplementary, UI null-safe). So notes ride the durable reload; live SET is boolean-only on the 8 charset-safe ids. **deploy#603 PR#604 = Aisha Idrissi** (`A.Idrissi/0603-over-merged-transport`): passthrough transport (no node-prop allow-list in deploy → columns land free) + post-load readback proof; `graph-flag-over-merged.yml` modeled on prune-orphans path, ids from committed `scripts/over_merged_narrator_ids.txt`, pre/post gates refuse-on-subset. **Prod = code-enforced fail-closed guard hard-refusing `env=prod` this wave (main#928 backup-gate) + production GH-environment approval = double-gated.** Revising to ids-only; new head SHA pending → then 2 deploy reviewers. **The 8 authoritative ids** (make_canonical_id via producer resolved_id, NOT deploy-derived): 9b793066 / afc30d02 / 5b41b11e / cb879ada / 0806983d / 3f7c1e28 / 49618f4b / cf44624b (all `nar:<id>`).

> **PROGRESS-CORRECTION 2026-07-14 (SUPERSEDES the "over_merged=true ONLY" call above):** Aisha's REVISED PR#604 (head `96b7733e`, all CI green, MERGEABLE) keeps `over_merged=true` **+ `over_merge_note`** in the live SET — but the note is a **single shared OPERATIONAL note supplied as the workflow `note:` input**, default a generic charset-safe explainer ("Over-merged canonical narrator: this node fuses multiple distinct transmitters…pending an external-evidence split") — NOT the per-node YAML notes. That default PASSES the `safe_literal` whitelist (`A-Za-z0-9 ._,:;/#()-`; no `"`/`>`/`%`), and the per-node distinct rijāl YAML notes still ride the durable reload (reload re-asserts idempotently). This is BETTER than my ids-only revision: consumer gets a meaningful tooltip on stg NOW, injection surface stays closed (whitelist), self-reconciling with reload. **ACCEPTED — do not force ids-only.** Reviews: **Nino Kavtaradze APPROVED** (security — verified the `env=prod` refuse is the FIRST executable ssh-script statement w/ unconditional `exit 1`, double-gated w/ `production` env approval, no prod-write path; whitelist rejects `'`/`\`/`$`/brace/newline on id+note charsets; creds inside container `sh -c` NEO4J_AUTH never on argv; `test_committed_id_file_is_inert` pins placeholder→0; TechDebt none, concurrency-group obs already tracked deploy#576). **Lucas Ferreira** (SRE) in flight. **CORRECTION on the 8 ids: the `9b793066…` etc. above are 8-hex PREFIXES (shown w/ ellipsis), NOT wireable full `nar:<uuid>` ids.** Seed entries are name-keyed (no explicit `canonical_id`) → `over_merged_flag.py:96` derives each via `make_canonical_id(normalize_arabic(name_ar))` (pure/stable, da#376 drift closed). **Kavitha emitting the full 8 `nar:<uuid>` ids** from that exact derivation (producer-owned, NOT deploy-derived) → wire into `scripts/over_merged_narrator_ids.txt` → stg dry-run (validates 8/8 present) → real SET. Local da main was 20 behind origin (stale-child hazard) — the stage lives at `src/resolve/over_merged_flag.py::apply_over_merged_flags`, registered in `__init__.py` RESOLVE step order.

> **PROGRESS 2026-07-14 (transport MERGED + arming in flight):** **deploy#603 PR#604 MERGED** (`d6a900ff`, #603 CLOSED) — Nino Kavtaradze (security) + Lucas Ferreira (SRE) both APPROVED at head `96b7733e`, neither author (Aisha). Lucas verified cypher-shell 5.x stdin (`:param`+one `;`-stmt, no `:auto`), airtight silent-zero (`extract_int`+`-z` hard-fail, real-run refuse-on-subset, SET+post-verify both asserted ==EXPECTED), loader passthrough (no allow-list) + non-fatal readback, `git fetch||exit 1` before `reset --hard`. Merged INERT (placeholder id file). **The 8 FULL authoritative ids now confirmed** (Kavitha via production `load_over_merged_seed()`+`OverMergedEntry.resolved_id()`, all UUID5): `nar:9b793066-94f6-5f99-ae1c-b2636515fa9b` (عبد الله) / `afc30d02-f7ed-5d09-bc3b-4535a27dc0fb` (سفيان) / `5b41b11e-b18a-5c18-b6e9-2091cb7d3cdf` (أبو عبد الله) / `cb879ada-0dac-575a-a071-ffe2d265129c` (أبو جعفر) / `0806983d-8571-5a6f-b22e-62647a71cd4c` (محمد) / `3f7c1e28-2632-5214-bbec-c17ac5c021a9` (أحمد) / `49618f4b-7a1b-5c35-8c95-5b837b965857` (عبد الرحمن) / `cf44624b-1f1b-5c9e-b498-625d55191fd2` (أبو بكر). Also in scratchpad `over_merged_8_ids.txt`. **ARMING = deploy#605** (Aisha, branch `A.Idrissi/0605-arm-over-merged-ids`): wire the 8 ids into `scripts/over_merged_narrator_ids.txt` + flip `test_committed_id_file_is_inert`→assert exactly-8 (keep subset-refuse pinned). Split from the mechanism PR so the live-affecting arming gets focused review. **After #605 merges: ORCHESTRATOR dispatches `graph-flag-over-merged.yml` on stg `dry_run=true` (validates 8/8 present) → real (assert flagged==8). PROD HELD (code-enforced `env=prod` refuse + backup-gate main#928).**

> **SUPERSESSION 2026-07-14 — ARMING IS deploy#606, NOT #605 (cross-spawn collision):** Two Aisha sessions ran divergently. I spawned a fresh Aisha (from the compacted summary, not seeing the original was still working) to ARM via **#605** (`A.Idrissi/0605-arm-over-merged-ids` @ 14de1c8: wire 8 ids into the `.sh`+ids.txt design, flip the inert test). Meanwhile the ORIGINAL Aisha finalized a SUPERIOR design in **#606** (`A.Idrissi/0603-over-merged-transport` @ `9c1218ca`, created 02:28 AFTER #604 merged): **deletes** `flag_over_merged_narrators.sh` + `over_merged_narrator_ids.txt`, **adds** `flag_over_merged_narrators.py` + `over_merged_narrator_flags.json` (the 8 REAL da#448 per-node notes byte-identical, apostrophes/quotes intact). Flags via `UNWIND $rows AS r MATCH (n:Narrator {id:r.id}) SET n.over_merged=true, n.over_merge_note=r.note`; `$rows` is a **bound** `:param` (r.note reaches SET as data, never re-parsed). **Injection model = whitelist ids (`^nar:[A-Za-z0-9:_-]+$`) + ESCAPE notes** (`_cypher_str`: `\`→`\\` THEN `'`→`\'`, control chars encoded/refused) — correct + complete for a single-quoted Cypher literal (I verified the ordering). Per-id read-back NAMES any id matching 0; post-SET count asserted ==8. **#606 SHIPS, #605 ABANDONED** — #606 delivers the real per-node rijāl notes on the LIVE stg graph now (not a shared generic note, not deferred to reload). #606 = strictly better deliverable; escape-correctness risk well-handled + reviewable. **#605 branch landed on origin (no PR) as a local fallback — DELETE (branch+worktree, remote+local) after #606 merges.** #606 reviewers: Nino Kavtaradze (security — re-verify escape==whitelist-safe) + Weronika Zielinska (mechanics + byte-identity vs da seed). CI green (CLEAN). Dispatch prereq now = **#606** merge, not #605.

> **PROGRESS 2026-07-14 (#606 MERGED, stg dry-run caught a Cypher defect → #607):** **#606 MERGED** (`5a72bfe0`) — Weronika Zielinska (mechanics + byte-identity: 8 notes exact vs da seed, lengths 406/292/241/147/162/104/98/360) + Nino Kavtaradze (escape model: ran `\'`, `x'; MATCH(n) DETACH DELETE n //`, lone-backslash, newline, `}]` payloads through `_cypher_str`, all decode verbatim with clean closure; ordering regression-locked) both APPROVED, neither author. #605 remnants DELETED (worktree+local+remote branch); arming issue #605 CLOSED (satisfied-by-#606). deploy main now has `flag_over_merged_narrators.py` + `over_merged_narrator_flags.json` (`.sh`/`ids.txt` gone). Workflow inputs now just `env`+`dry_run` (no note; notes from JSON). **stg DRY-RUN DISPATCHED → FAILED at precheck** (run 29219977687, read-only so NOTHING written): `_PRECHECK_QUERY` `RETURN r.id + ' matched=' + toString(count(n))` mixes grouping key `r.id` with aggregate `count(n)` → Neo4j "Aggregation column contains implicit grouping expressions. Illegal expression(s): r.id". **Both reviewers + green CI missed it because the unit tests validate the built STRING, not that Neo4j ACCEPTS the query** — the manual stg dry-run was the only real instrument (see [[feedback_query_builder_test_needs_real_engine]]). `_SET_QUERY`/`_POSTCOUNT_QUERY` fine (count stands alone). **Fix = deploy#607** (Aisha, `A.Idrissi/0607-precheck-cypher-fix`): rewrite precheck with `WITH r.id AS id, count(n) AS matched RETURN id + ' matched=' + toString(matched)` + ADD a Neo4j integration test (ephemeral neo4j:5-community via integration-tests/docker-compose.test.yml) executing ALL 3 phase queries — the guard that would have caught it. **After #607 merges: re-dispatch stg dry-run (the real instrument) → real per-id SET. PROD HELD.**

> **DECISION 2026-07-12 — LIVE OP IS BOOLEAN-ONLY (deploy#608); SUPERSEDES the per-id-notes #606/#607 path. FINAL call — stop re-litigating it.** After the #606 dry-run caught the invalid precheck Cypher, the whole per-id-notes-in-the-live-op design was reverted. **#608** (Aisha Idrissi, branch `A.Idrissi/0603-over-merged-transport` @ head `0c87c9cd842962798c616946378ec12677284999`) makes the live op **boolean-only**: `MATCH (n:Narrator) WHERE n.id IN $ids SET n.over_merged = true RETURN count(n)` — no `over_merge_note` on the live graph. **Deletes** `flag_over_merged_narrators.py` + `over_merged_narrator_flags.json` (404 at head — the #607 invalid-precheck machinery is GONE, so #607 is resolved-by-removal, not patched); **restores** shellcheck-linted `flag_over_merged_narrators.sh` (ids-only `safe_literal` whitelist) + `over_merged_narrator_ids.txt` with the 8 authoritative da#448 ids (byte-identical to scratchpad set at origin). **Why boolean-only is correct (durable principle — do NOT flip back):** free text does NOT belong threaded through a shell→cypher whitelist; the per-node notes' proper typed home is the **parquet reload** the producer already writes (da#447); `over_merged=true` alone is complete interim disclosure (consumer badge/explainer degrades gracefully on null note, ig#1183); the trivially-valid boolean SET + the stg **dry-run as the real-engine gate** dissolves the whole [[feedback_query_builder_test_needs_real_engine]] risk class. The #607 agent independently reproduced the invalid-Cypher error against live neo4j:5-community before standing down (corroboration). **Verified at origin by orchestrator:** boolean-only op, `env=prod` hard-refuse FIRST executable stmt (unconditional exit 1) + `environment: production` double-gate, stale-checkout guard, empty-set fail-safe + refuse-on-subset, unparseable-count hard-fail, 8 ids byte-match. **Reviewers: Nino Kavtaradze (security) + Lucas Ferreira (SRE) both APPROVED at `0c87c9cd`**, neither author; Nino called it a net risk reduction vs #606 (drops the free-text injection surface entirely). #607 worktree/branch DELETED. **Next: merge #608 (--merge) → close #607 issue as resolved-by-removal → re-dispatch stg `dry_run=true` (validates 8/8) → real run (assert flagged==8). PROD HELD — code-enforced `env=prod` refuse + backup-gate main#928; NOT built, NOT authorized.** Non-blocking follow-up: CI integration test exercising even the boolean op against ephemeral neo4j (no issue exists yet; dry-run already covers the gate).

> **DONE 2026-07-12 — STG FLAG-NOW COMPLETE + VERIFIED.** #608 MERGED (`255b5d96`), #607 CLOSED (resolved-by-removal). stg **dry-run** (run 29220814769) at merge SHA `255b5d9`: `configured: 8 | present in graph: 8`, no Cypher error (boolean op valid — #607 class dissolved). stg **real run** (run 29220843300, `dry_run=false`): `SET over_merged=true on 8 node(s)` → `FLAG_RESULT outcome=flagged configured=8 matched=8 flagged=8`, post-count read-back verified all 8 carry `over_merged=true` (hard-fails otherwise). **All four legs shipped on stg:** producer da#447/#448, consumer ig#1184, transport deploy#604, live-op deploy#608. The honest leaderboard is now live-flagged on stg (8 over-merged hubs annotated, betweenness disclosed-in-place, never dropped). **PROD STILL HELD** — the op hard-refuses `env=prod` (backup-gate main#928); prod flag NOT built, NOT authorized. Remaining non-blockers: ig#1185 (per-hadith chain endpoint over_merged coverage), the parked ~7.5h resolve re-run (makes the flag + per-node notes durable via reload), da#446 (attested-death pollution).

> ## 2026-07-12 — da issue triage (21 open) COMPLETE + batched resolve-re-run plan APPROVED (owner "let's go"). Wave launching.
>
> Full triage of open da issues vs current code + the loaded `bd133e6` artifact. **Closed as fixed:** deploy#419/#426/#417 (#418 dup) prune-safety (anchored consumer-key regex, COMPLETENESS binding, post-readback gate — all at deploy HEAD `5c1a0e8`); **da#345** (ʿĀʾisha chimera — fixed by da#356 *in-artifact*, verified live on enriched stg: no `عاءذه` node exists, عائشة is rank-3 at 33,210).
>
> **The da#337/#346 over-merge is the remaining gate on the main#928 choke-point deliverable** — top betweenness on stg is bare mononyms (`nar:9b79…` bare ʿAbd Allāh ≈3.87M; `nar:afc3…` bare Sufyān ≈3.63M with impossible *sahabi + d.278 AH* metadata = signature of a fused node). Fix = **isnad-neighbour context discriminator** (Jean-Claude design): cluster an over-merged node's mentions by shared *specific* chain-neighbours (teacher/student at pos±1 within `(hadith_id, chain_index)`), peel well-supported/well-separated communities onto their own id. **Pure SPLIT** (never merges distinct nodes → mention_count conserved → the *safe* direction per [[feedback_drop_gate_bidirectional_ab]]). Hard prereq **da#411** (splitter `_build_chain_index` `narrator_split.py:506,516` keys on hadith_id ALONE; loaders correctly use `(hadith_id, chain_index)` `load_edges.py:129-140` — multi-isnad hadiths fabricate cross-chain neighbours that corrupt both date and context signals; must land first).
>
> **Batched resolve re-run — restart at `ner`, ~7.5h, ~$0.01** (see [[feedback_sweep_expensive_stage_before_launch]]): **PR-A #411** (chain-index prereq) → **PR-B #337/#346** (context split driver) → **PR-C #397/#398** (bio_promote truncated-merge guard refine; carries bidirectional-unweighted A/B + Arabic reviewer) → **PR-D #385** (count the accepted truncated-residue residual) → **PR-E #347a** (normalizer fold `ى→ي` + drop standalone `ء`; Kavitha blast-radius: 192 clusters, **~1,689 mentions actually move, 0 distinct-person collision**; extend the mint-time `canonical_surface` fold `src/utils/arabic.py:597-626`).
> **Parallel — gate the reload, NOT the re-run:** **#382** (collection_coverage passes a 0-of-650,986 load because NULL expected_count is skipped then reported all-within-threshold; fix red-first) · **#364** (fix the 4 false comments ONLY — Alejandra: loading mis needs a name-reconciliation crosswalk, only **10 of 2,077** mis narrators map, rest are Latin translit + 80 `NotMapped-N` sentinels forming a disconnected component that never touches the leaderboard; the load itself is a separate follow-up, NOT this cycle) · **#370** (tag-and-keep: mark the 42,770 bio-only narrators `attestation=bio_only`; owner "tag & keep").
> **DEFERRED behind their OWN bidirectional-UNWEIGHTED A/B + Arabic rijāl reviewer** — the [[feedback_drop_gate_bidirectional_ab]] danger direction (these MERGE): **#347b** (bare↔full, Kavitha: 1,380 "candidates" sized by the mention-weighted metric that hid the da#423 deletions = unreliable, + 1,522 genuinely ambiguous e.g. محمد 6,275 extensions) · **#431** (kunya↔ism, ~3,204 kunya nodes / 609k mentions, pervasive; Abū Wāʾil `ابو واءل` 3,117 mc separate from شقيق بن سلمه 364 mc) · **#364** mis crosswalk + load.
>
> Implementers + reviewers from the **da roster** (child-repo rule [[feedback_child_repo_implementer_rule]]); each in its own da child worktree (NOT `Agent isolation:worktree` — that worktrees the parent, [[feedback_subagent_worktree_wrong_repo]]). **Prod HELD** — wave ends at a verified stg leaderboard; prod promotion is a separate owner gate (backup-gated, [[project_backup_restore_logrotate_gap]]).

> ## 2026-07-12 — STG RELOAD (from-parse re-run) PRUNED + ENRICHED, at artifact parity. Prod still HELD (backup-gated).
>
> The main#928 re-run-from-`parse` artifact `staged/narrator-resolve/2026-07-12-bd133e6` (canonical **107,951**, `parquet_md5=4292a96…`, run_status=complete) is loaded → pruned → enriched on **stg**:
> - **Prune** (deploy `graph-prune-narrators.yml`, run 29205112218, dry_run=false): 209,944 → **107,951**; **101,993** orphans DETACH DELETEd; **0 missing, 0 orphans remaining**; post-verify exact (`107951 (pre 209944, expected 107951)`).
> - **Enrich** (deploy `graph-ops.yml` op=enrich, run 29205195180): betweenness on **all 107,951**; pagerank + in/out-degree; louvain **47,079** communities; post-op gate PASSED. Top choke-point betweenness ≈3.87M `nar:9b793066…`. (`name=None` in the enrich metrics log is a log-join artifact — prune removes, enrich adds, neither relabels.)
> - **Two locale bugs fixed en route to unblocking the prune** — both the `[!-~]`→`[[:graph:]]` collation trap (GNU sed evaluates a bracket RANGE by locale collation; en_US.UTF-8 on the VPS drops `/ - .`): `verify_prune_provenance.sh` (deploy#599/PR#600) and its sibling `graph_load_provenance.sh` (deploy#601/PR#602). Both merged, RED→GREEN proven via localedef-built en_US.UTF-8. See [[feedback_passing_repro_masks_bug]].
> - The corrected artifact **fixes the da#356 broken election** (chimeric labels) documented below — this reload is the point where the stale/polluted stg state is finally replaced.
>
> **Prod: NOT touched, on HOLD.** Prod prune stays backup-gated ([[project_backup_restore_logrotate_gap]] — backups have never successfully run; B2 backup bucket empty). Do NOT dispatch prod load/prune without explicit owner go + a real verified-green backup.

> ## 2026-07-09 — OWNER DECISIONS + THE PROGRAM (tracking: main#928)
>
> Owner: *"fix all the defects we've discovered trying to get this to stg, and restart the pipeline from 'the point of the earliest defect'"* and *"I am unbothered by a timesink or token usage complication, the more correct and repeatable the better right now."*
>
> **Restart point = `parse`** (da#353 is the earliest defect; `acquire` is clean).
>
> **Three owner rulings, 2026-07-09:**
> 1. **da#356 — do BOTH fixes.** (a) identity invariant: canonical id/name is a function of the **mention's** normalized form, never a bio candidate's; bio *enriches only* (dates, gender, external_id) and may not rename or re-key. (b) corroboration gate on Stage-2 fuzzy bio-match. Variant-spelling merge moves to `fuzzy_cluster` (mention-to-mention), whose `_choose_representative` ranks by `mention_count` — the dominant *attested* spelling wins. **`fuzzy_cluster` is the cure, not the disease** (my original hypothesis blamed it; backwards). Fix (a) alone would trade over-merge for fragmentation (da#347) — hence both.
> 2. **deploy#558/#559/#560 gate the PROD wipe.** Owner authorized wiping stg **and** prod ("prod isn't stable enough to merit surgery"), but backups land first. prod `user-postgres` holds accounts/sessions/`audit_log` — **not reconstructible from the artifact**, and `isnad-backup.timer` has never run on either host. Restore rehearsal must be shown **RED against a corrupted backup** before trusted.
> 3. **da#352 — a narrator attested only in a deduped edition IS real** ("I feel like it is"). So: do NOT filter `resolve` to the loaded population, and do NOT relax `orphan_narrators.cypher`'s `// Expected: 0`. Open question the ruling doesn't answer: *with what edges?* Hypothesis (routed to Jean-Claude, unverified): **da#352 and da#282 are one fix** — a dropped edition should contribute its isnad chain to the surviving canonical hadith, which requires modelling *multiple isnads per hadith* first-class; a naive remap flattens distinct isnads and makes da#282 worse. Load-bearing empirical question: **do dropped editions carry distinct isnads, or are they isnad-identical?**
>
> **Sequencing law (see [[feedback_sweep_expensive_stage_before_launch]]):** `resolve` is ~7.5h. A resolve-stage defect fixed *before* launch is free; one found *after* costs the whole stage again. **Launch gate = every known `parse` + `resolve` defect merged.** `load`/`enrich` fixes may land while resolve runs. Every defect found on this push was upstream of the load — none would have been caught by loading and looking.
>
> **da#356 site, exact:** `src/resolve/disambiguate.py:1199-1223` — `c = best.candidate; norm_name = c.name_ar_normalized …; canonical_id = _make_canonical_id(norm_name)`. Two lines. `_upsert_canonical` (`:882-936`) only fills *empty* fields, so it is not the site.
>
> **da#309 promoted to BLOCKING**, and my first two claims about it were WRONG (corrected on-issue): dedup **did** run (`parallel_links.parquet` = 6,720,435 rows), and **dedup does not deduplicate hadith nodes at all** — its only write target is `parallel_links.parquet` (`dedup.py:779,812`); node-level dedup is `canonical_matn_identity` in `load_nodes` (da#220). The real defect: on `ImportError` it writes a **schema-valid zero-row** parquet and the pipeline reports success ⇒ **zero `PARALLEL` edges** (prod carries ~4.49M) presented as a true negative. `sentence_transformers` 5.3.0 now imports on the resolve host, so leg 1 is green; leg 2 (fail-loud) is not.

Owner ask "find choke points of narrators in transmission chains" — code was da#326/#327 (sampled GDS betweenness+pagerank+louvain+degree via `enrich` CLI subcommand); only the RUN remained. Run stg-first via deploy `graph-ops.yml` (env=stg operation=enrich dry_run=false; loader image `noorinalabs-data-acquisition-load:stg-latest`).

**2026-07-08 — enrich RAN + VERIFIED on stg.** 150,187 narrators enriched (full coverage = canonical count). Two bugs surfaced+fixed along the way:
- **da#335/PR#336** — `write_audit_entry` crashed the whole run with `OSError: read-only file system: 'data'` AFTER `enrich_all` already wrote centrality to Neo4j (container fs is read-only by org convention). Fix: catch OSError, warn, return None (best-effort). NOT an OOM (the handoff's OOM warning was wrong).
- **deploy#544/PR#545** — graph-ops enrich verify echo queried `n.name_arabic` (nonexistent) → names logged as `None`. Fixed to `coalesce(n.name_ar, n.name_en)`. Narrator name props are `name_ar` / `name_en` (transliteration fallback via `_narrator_name_en`) / `name_ar_normalized` — there is **no** bare `n.name`.

**Plausibility check (named top-10, stg):** real mega-hubs surface correctly — الزهري al-Zuhrī, أبو هريرة Abū Hurayra, شعبة Shuʿba, سفيان Sufyān, سالم بن عبد الله Sālim b. ʿAbd Allāh → mechanism SOUND. **BUT the top is contaminated by over-merged generic-name nodes** (#1 أبو عبد الله 10.6M ≈2× Abū Hurayra, plus عبد الله / أبو جعفر / fragments عليم, ابن عمران) — same-kunya narrators collapse into one `nar:uuid5(normalized-name)` node, inflating betweenness. Entity-resolution problem UPSTREAM of centrality, same class as the P7 #723 name-quality work (da#247/#308/#311; retired memory, git history). Caveat: sampled betweenness magnitudes shifted between two runs (seed 42 but sampled≈approx) — trust RANK not magnitude.

**Owner decision 2026-07-08: HOLD prod enrich until the over-merge dedup is fixed** (so first prod choke-points are all real narrators). stg stays enriched as-is for internal validation. Gate = **da#337** (disambiguate generic-name narrators via ṭabaqa/teacher-student/co-occurrence; acceptance = re-run stg top-10 has no bare-kunya aggregate outranking al-Zuhrī/Abū Hurayra → then promote prod w/ parity). Do NOT run prod enrich until da#337 lands + owner re-confirms.

**2026-07-09 — da#337 resolve re-run COMPLETE + PUBLISHED; stg load BLOCKED.** Full 10-stage `isnad-ingest resolve --no-resume` ran 21:58→05:27Z (rc=0). `narrator_split` landed: canonical **150,187 → 129,234**, 179 nodes split, 194 peel records (`data/curated/narrator_splits.parquet`), 4,005 mentions remapped. **The gate criterion is MET at parquet level** — top-20 by `mention_count` is now real hubs (Abū Hurayra #1 54,610; Shuʿba #5; Sufyān #6; **al-Zuhrī #7 24,910**; Anas #10; Qatāda #11; Nāfiʿ #12; Mālik #15) with no bare-kunya aggregate outranking them. Published to B2 as `staged/narrator-resolve/2026-07-09-dcb6205` (26 objects, verified).

**stg load RAN 2026-07-09T17:04–18:06Z (run 29035731215) — graph MUTATED, `rc=1` on validation. The da#337 gate is NOT yet met, and a reload alone CANNOT meet it.** The B2 Class-B cap (4th blocker, deploy#556) cleared after the owner touched the console; the first three defects were already fixed (da#348/PR#349, deploy#552+#553/PR#554; loader `stg-latest` = `sha-394e8d8`). All four enumerated in [[reference_pipeline_b2_publish_key]].

What the load did: 26 parquet / 1.1 GB pulled, `Narrator created=10,427 merged=118,807` (= **129,234**, the da#337 canonical count — the artifact and the ingest are both correct), 1,563,783 nodes / 235,039 new edges.

**Why the gate is not met — two independent reasons:**
1. **`betweenness_centrality` on stg is STALE** (from the 2026-07-08 enrich on the *old* graph). A data load does not recompute centrality. Exactly 150,187 narrators carry a betweenness value and 10,427 carry none (150,187+10,427 = 160,614 ✓). So the betweenness top-10 still shows the OLD failing ranking — `أبو عبد الله` 1.06e7, ~4.6× Abū Hurayra. **The gate metric is betweenness, so it currently reads FAIL for a stale-property reason, not a data reason.**
2. **The loader is MERGE-only, so the dedup's 31,380 collapsed ids were never deleted** — graph ends at 160,614 narrators, not 129,234 (see [[reference_graph_ops_cypher_shell]] §4). Re-running enrich over that union of old+new topology would produce a meaningless top-10.

**`mention_count` at graph level matches the parquet top-20 exactly** — Abū Hurayra 54,610 · Shuʿba 29,280 · Sufyān 28,323 · al-Zuhrī 24,910; `أبو عبد الله` is genuinely split into two nodes (mc 10,697 and 90). da#337's *split* mechanism works.

> **2026-07-09 — THE TOP-10 GATE IS NOT EVIDENCE. `name_ar` LABELS ARE CHOSEN BY A BROKEN ELECTION (da#356).** Three of the top ten are **chimeras** of several distinct people, labelled with the least significant member's name:
> - **`عائذة` mc 35,436 IS ʿĀʾisha.** `عائشة` has **zero** canonical nodes; the raw corpus carries her 36,603× (`عَائِشَةَ` 35,979 + `عائشة` 624). Her mentions' `name_raw` is correct; she is demoted to an *alias* of an obscure `عائذة امرأة من بني أسد`.
> - **`ابن عبس` mc 50,962 IS Ibn ʿAbbās** (+ `ابي عبس`, `ابن كعب`, `ابن عوسجه` — ≥5 people). `ابن عبس` occurs **0×** in the raw corpus.
> - **`الأعلم` mc 19,296 IS al-Aʿmash** (+ `المعلم`, `الأعمى`).
>
> **Mechanism (corrected — my first guess was WRONG):** NOT `fuzzy_cluster`, NOT `_choose_representative`. It is **`disambiguate` Stage-2 fuzzy bio-match**. `merge_log.parquet` shows `merge_stage=fuzzy` for 35,434/35,434 of ʿĀʾisha's mentions. The **itqan rijāl DB carries OCR-corrupt spellings** (`عاءذه`, `ابن عبس`, `الاعلم`) and NOT the correct forms (0 exact bio candidates). A real mention finds no exact bio → `_fuzzy_match_blocked` → matches the corrupt bio at `fuzz.ratio>=80` + `Levenshtein<=2` (one Arabic letter; `fuzz.ratio("عاءشه","عاءذه")==80.0`). `disambiguate.run:1203,1223` then keys the mention on the **candidate's** name and stamps its `name_ar` (`:1250`), demoting the real spelling to alias. Hence `source_corpus: fawaz` vs `source_ids: itqan:*`. **Refuted:** the uuid5-tiebreak theory — `_upsert_canonical:912` increments `mention_count` per mention, so it IS populated at cluster time.
> **Systemic: 14,223 chimeric nodes / 593,314 mentions** (detector: node whose `name_ar_normalized` never appears among its own mentions). Top-20 chimeras also include **Ibn ʿUmar→`ابن عمران` (r4), ʿAlī→`عليم` (r14), Ibn Shihāb→`ابن شبل` (r17), Maʿmar→`معتمر` (r19)**. Shuʿba/Sufyān/al-Zuhrī/Nāfiʿ/Mālik/Qatāda clean.
> **da#346 (bare `عبد الله`) and da#347's Anas under-merge are ORTHOGONAL** — `_MIN_SHARED_TOKENS=2` blocks bare-mononym cluster merges. **A global similarity loosen would worsen the 593k chimera class and still not fix Anas. Do not tune a threshold.**
> Fix needs an owner call: (1) stop re-keying a mention onto the bio candidate's name (bio enriches only) — re-keys ~593k mentions; or (2) gate fuzzy bio-match on corroboration (date-band / chain-neighbour). Both are upstream of the wipe.
>
> **`normalize_arabic` is NOT at fault**: `normalize_arabic("عَائِشَةَ") == "عاءشه"`, shīn preserved. Verified.
>
> **Every top-10 "quality" check we ran — including the 07-09 one I called clean — read a label produced by this election.** `أبو هريرة`/`الزهري` happened to be labelled right, so the list looked plausible. **Do not load or enrich before da#356 lands**: betweenness over a vertex that fuses five people's edges is exactly the choke-point artefact we set out to remove. da#345 and half of da#347 are *answered* by da#356 (over-merge, not normalization); `قَتَادَةَ` (empty `source_ids`+`aliases`) is a separate pure label-selection case.

**Validation `rc=1` — 3 FAILs, triage:** `chain_integrity: 100 cycles` (known, da#248); `transmitted_to_hadith_ref: 3` (known, da#325); **`orphan_narrators: 70,855`** (= zero-degree narrators; the real signal). Of those, **65,840 pre-date this load** (they carry a betweenness value; the top are `mention_count=0, betweenness=0.0` reference-catalog biographical entries never in a chain) and only 5,015 are newly created — the #723 baseline tail was 44,073, so the dedup contributed ~26.8k, NOT the whole 70,855. Do not attribute the full orphan count to da#337. Edge `created=0` on PARALLEL_OF / APPEARS_IN / GRADED_BY is **benign** (MERGE-matched existing): graph holds PARALLEL_OF 4,490,659 · TRANSMITTED_TO 2,679,527 · APPEARS_IN 775,916 · NARRATED 581,094 · STUDIED_UNDER 92,560 · GRADED_BY 48,283.

**Next action (needs an OWNER decision — both steps mutate stg, neither is started):**
1. **Prune** the orphan narrators (id ∉ new canonical set → `DETACH DELETE`). Per [[feedback_iac_over_oneoffs]] this belongs in `graph-ops.yml` as an operation (a small deploy PR), not a box one-off — same shape as the fawaz migrate+prune. Decide first whether to prune only the ~31,380 dedup-collapsed ids or the whole zero-degree tail (the 44,073 pre-existing catalog orphans are arguably legitimate reference nodes).
2. **Re-run enrich** (`graph-ops.yml`, env=stg) to recompute betweenness on the pruned graph. Only then is the top-10 gate test meaningful.

Prod still HELD (owner-gated; stg-verify first). Also seen: loader emits `collapsed_double_corpus_prefix` at **16M+ occurrences** (`sanadset:sanadset:…` → `sanadset:…`) — the BATCH half of the hadith-id double-prefix defect (ig#63/#72 fixed only the streaming half; retired memory, git history) is live in the published artifact (ig#63/#72 fixed only the streaming half). Filed **da#353**. The `src.parse.identity` guard normalizes it so the load is not corrupted, but the parquet still carries the doubled form.

**da#353 root cause CORRECTED 2026-07-09** (original diagnosis was wrong; it is NOT "batch skips `hadith_node_id`"). `data/raw/sanadset/sanadset.csv` header is `Hadith,Book,Num_hadith,Matn,Sanad,Sanad_Length`; the parser looks for `hadith_id|id|Hadith_ID` and `book_id|book|Book_ID` → **both null in 650,986/650,986 rows** (76% of the 853k corpus). `books.csv` has only a `Book` column (956 names, **no id**) so `_parse_books` returns `{}` → `has_books=False` → **da#219 never activated**. `collection_name` then falls back to the CSV stem `"sanadset"` == `_SOURCE_CORPUS`, so `source_id = sanadset:sanadset:0:0:{row_offset}` — a *legitimate* `corpus:collection` id that only **looks** double-prefixed. `_collapse_double_corpus` eats the collection segment (16M+ warns) but introduces **0 collisions** (650,986 → 650,986 distinct) — nothing is corrupted today. Real harm: the id is **positional** (re-scrape ⇒ everything re-keyed), all 650,986 land in ONE Collection, and `hadith_number`/`book_number` are NULL for 76% of the corpus. `(Book, Num_hadith)` is a **perfect unique key** (650,986 distinct, 0 dups, 956 books). **The re-run therefore starts at `parse`, not `resolve`.** Guard's false-positive design filed as **da#355**.

**da#352's 14,884 root-caused 2026-07-09:** `resolve` extracts mentions from **all** staging hadiths, but `load_nodes` deliberately drops 74,986 (da#191 `is_canonical_hadith` non-canonical editions + da#220 matn-identity dedup). Narrators attested **only** inside dropped editions become edgeless canonical nodes. Replaying the predicate: 776,242 kept / 74,906 dropped → **14,988 canonical narrators appear only in dropped editions** vs 14,884 measured (104 residual, likely `STUDIED_UNDER` from name-keyed `network_edges_*.parquet`, unverified). Mentions are clean at artifact level (**0** dangling hadith_ids). This **also explains the untriaged `missing_endpoints`** (`APPEARS_IN 75,380` ≈ the skip count): edges whose hadith endpoint was deliberately skipped. Expected, not a data defect — a *reporting* defect (da#354). Owner call needed: filter resolve to the canonical hadith population (cleaner invariant) vs keep them and fix `orphan_narrators.cypher`'s `// Expected: 0` contract.

**Defects found 2026-07-09 and now filed:** deploy#557 (no prune op — MERGE-only load can't shrink), da#351 (load doesn't invalidate enrich centrality), da#352 (orphan_narrators 70,855, three populations conflated), da#353 (batch doubled corpus prefix), da#354 (`load` rc=1 conflates load-failure with validation-findings). **da#248 REOPENED** — its cycle symptom persists post-PR#264, and `chain_integrity` caps at `LIMIT 100` so the true cycle count is UNKNOWN; run `queries/analysis/chain_integrity_mononym_ranking.cypher` uncapped (ideally after the prune) before any further fix work. Untriaged, deliberately not filed: large `missing_endpoints` (PARALLEL_OF 2,229,776 · STUDIED_UNDER 238,831 · APPEARS_IN 75,380 · GRADED_BY 21,185; Hadith `skipped=74,986`) — unknown whether expected; and the 3 dangling `transmitted_to_hadith_ref` residual after da#325 closed.

Residual anomalies filed, NOT started (owner deferred to quota reset): **da#345** `عائذة` rank-3 at 35,436 (mis-normalized `عائشة`? or unpeeled over-merge); **da#346** bare `عبد الله` 19,412 (over-merge `narrator_split` declined — likely no date-band to discriminate on; needs isnad-neighbor context); **da#347** Anas b. Mālik **under**-merged across `أنس بن مالك` 18,968 / `أنس` 17,983, plus normalization artifacts (`ابن عبس`, `الأعلم`, diacriticized `قَتَادَةَ`). Note #346 and #347 pull in opposite directions (over- vs under-merge) — quantify before changing thresholds.

Also 2026-07-08: **ig#1179/PR#1180** fixed the staging-deploy embed race — `notify-deploy needs:` widened to `[build-and-push, build-and-push-embed]` (embed is a standing deploy gate since deploy#523; gating on api+frontend only raced the embed publish → tripped deploy-stg's deploy#418 manifest preflight). Ontology overlay `repos/isnad-graph.yaml` corrected to match (dispatch @v4, embed gate). Related: [[feedback_iac_over_oneoffs]] (graph ops via graph-ops.yml, not box one-offs), [[project_donor_readiness_wave]].
