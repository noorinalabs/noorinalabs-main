---
name: project_semantic_embedder_parity
description: prod+stg semantic search API queries with HashingEmbedder while corpus embedded with MiniLM → 200-with-garbage; stg-gate implication.
metadata:
  type: project
---

Semantic search on prod (criterion 2 of Phase 8) has a silent embedder-parity gap co-verified during the ig#1148 review (PR #1164, Jun-Seo Park verdict):

- `noorinalabs-deploy/compose/docker-compose.prod.yml` `isnad-graph-api` sets **no `EMBEDDING_MODEL`** and runs the **torch-free** image → API embeds queries with the lexical `HashingEmbedder` (config default `hashing`).
- The corpus re-embed job bakes `EMBEDDING_MODEL=paraphrase-multilingual-MiniLM-L12-v2`.
- Both are `vector(384)`, so a hashing query cosine-compares against MiniLM corpus vectors with **no dimension error** → **HTTP 200 with a meaningless ranking**, not a visible failure. A plain "200 + non-empty" probe passes on garbage; the keyword-assertion smoke check added in PR #1164 is what surfaces it.
- The compose `api` block is **identical across both VPS boxes**, so **stg's API is also on hashing** — the mismatch bites any env whose corpus is re-embedded with MiniLM while the API stays torch-free.

**stg-gate implication:** an apparent stg "semantic search works" may be silently returning garbage-but-200, so it is **not** a validated gate for the semantic criterion until embedder parity is fixed. Directly qualifies [[feedback_stg_gate_before_prod]].

**Fix (promotion-window, owner-gated, apply in lockstep):** either give the API a model-capable image + matching `EMBEDDING_MODEL`, or re-embed the corpus with `hashing`. API image+model and corpus embedder MUST change together — flipping one regresses consistent-lexical → inconsistent-garbage. Tracked: **deploy#523** (bug/infra, boarded). Distinct from the closed prod re-embed cutover deploy#470. Child-repo detail in isnad-graph memory `project_semantic_embedder_parity_prod.md`.

**RESOLUTION (2026-07-06) — Option 1 chosen (model-capable API image), stg DONE, prod GATED on ig#1174.**
- **deploy#523 CLOSED / PR #525 MERGED** (`ae758d7`): compose `api` repointed to the model-capable **`-embed`** image via a durable IaC seam — `image: ${API_IMAGE:-…-isnad-graph-embed}:${IMAGE_TAG:-latest}` (fixed state IS the compose default; survives `write-deploy-env` `.env` truncation), `EMBEDDING_MODEL=MiniLM`, `--workers 4→2`, `mem_limit 2G→4G`, healthcheck `start_period 60s`. Reviewers Aisha Idrissi + Nino Kavtaradze approved. Rollback is IaC-only (git revert / one-line default flip / `rollback.yml`); box `.env` edits are ephemeral.
- **stg VERIFIED-GOOD**: api container = `embed:stg-latest` + `EMBEDDING_MODEL=MiniLM`; in-container prod `PgClient` cosine query returns topically-correct top-5 for patience/prayer/charity (embedder=`SentenceTransformerEmbedder`, corpus=33,958 embeddings). **Memory gate PASS**: measured **~1.5G/worker** (peak RSS 1,549 MB incl. forward pass) → projected container peak **~3.2G < 4G cap**. So stg IS now a valid semantic gate.
- **prod NOT yet cut over** — owner decision 2026-07-06: **scan embed first, then promote** (safety over speed; prod semantic search stays broken meanwhile). Blocking prereq = **ig#1174** (wire Trivy CVE-scan into `build-and-push-embed`; Linh Pham implementing). After ig#1174 merges → embed rebuilds+scans → promote to prod **with embed explicitly included** (`images=…,embed`, else no `embed:prod-<sha>` exists — deploy#470 opt-in promotion). ig#1174 detail: embed shares the scanned api image's pinned base; gap is the torch/ML layer's scan gate only.
- **Follow-up filed: deploy#527** — smoke check 8 (the parity CI guard, ig#1148) hits the auth-gated `/api/v1/search/semantic` unauthenticated → always 401, so it can't actually verify ranking; needs token-minting or a DB-side check. The guard is currently non-functional (had to verify ranking out-of-band).

**CUTOVER COMPLETED but INCOMPLETE — prod corpus was NEVER provisioned (2026-07-07).**
- **Prod embedder IMAGE cutover DONE**: promote.yml run 28833280888 (images incl. `embed`) retagged + deploy-prod run 28836495496 rolled out. Prod api now `ghcr.io/noorinalabs/…-embed:prod-3af4d39`, `EMBEDDING_MODEL=MiniLM`, model loads from baked `/opt/hf-cache` (no network), container ~461 MiB warmed, host 8.3 GiB available — memory fine, IaC rollback seam intact.
- **BUT prod semantic search is NON-FUNCTIONAL**: prod Postgres DB `Isnad-Prod` (dsn `postgres:5432/Isnad-Prod`) is **entirely empty** — no `isnad_graph` schema, no `hadith_embeddings`, no `hadiths`. Route degrades to **503** (ig#1110), NOT 200-with-garbage. The "200-with-garbage" framing was the stg/general risk; **prod's real state was `deploy#470` "prod unprovisioned"** all along. stg's same-named `Isnad-Prod` DB HAS the corpus (33,958 rows) — that's why stg verified good; prod ≠ stg on the Postgres corpus.
- **REMAINING STEP (unblocked): run `reembed-corpus.yml` env=prod** with `embed:prod-3af4d39`. Historical blocker was the workflow's guard refusing a stg-tagged image on prod — no `embed:prod-*` existed until this promotion minted one. `isnad embed-hadiths` (src/enrich/embeddings.py) reads Hadith matn from **Neo4j** (prod graph populated, 776k), `CREATE TABLE IF NOT EXISTS` the pg tables, writes both `isnad_graph.hadiths` + `hadith_embeddings` → self-sufficient. ~60–90 min, production-gated. Only AFTER this does prod semantic search actually work.
- Sibling graph bugs found same session (both prod+stg): **da#325** isnad chain empty for every hadith (TRANSMITTED_TO.hadith_id bypasses `hadith_node_id()` → 0 id-intersection), **da#326** narrator centrality/community enrich never applied (no choke-points). Owner sequencing: graph fixes AFTER embedder cutover fully done.

**CORPUS SCOPE INVESTIGATION (2026-07-07) — first prod re-embed timed out; corpus is far bigger than assumed; canonical-first plan chosen.**
- **reembed-corpus.yml prod run 28838450843 FAILED = SSH command_timeout (90min), NOT a data error.** `isnad embed-hadiths` ran the full 90min and the drone-ssh (`appleboy/ssh-action`) session was killed mid-embed; `reindex-embeddings`+`verify-recall` never ran. The remote embed process was **orphaned but kept running detached** (idempotent `ON CONFLICT DO UPDATE` upserts, so safe). Measured embed rate **~640 rows/min** (CPU-only, no GPU on the 15GiB/8vCPU VPS).
- **Graph is 776,239 text-bearing Hadith nodes** (prod AND stg identical count) — NOT the ~150k I assumed. Of these, **`sanadset` = 650,983 (84%)**. Full embed = **~20 hours**. stg was ALSO never fully embedded (only 33,958 = 4.4%) — the stg "gate" validated the embedder *path* on a partial slice, never a complete corpus. So there is no real prod↔stg parity target for the corpus.
- **`sanadset` is NOT pollution** (corrects any earlier "noise" framing): avg matn 432 chars, 98.7% distinct (642,470 unique), only 0.2% fragments — legitimate tafsir/athar hadith content. Matns are clean (consistent with #723 matn-scrub 0.000%). The "graph is a mess" complaint = the **graph relationships** (empty chains da#325, no choke-points da#326), NOT the matn corpus. BUT at 84% of vectors, `sanadset` would dominate semantic top-results → poor demo surface. `/search/semantic` has NO server-side collection filter (facets are projected for client-side refinement only), so scoping the demo to canonical needs either embed-time exclusion or a new server-side filter.
- **Owner decision (2 AskUserQuestions): "investigate sanadset first" → then "canonical now + sanadset job in parallel".** Plan: embed **canonical-only (~125,256 = 776,239−650,983, all named collections except sanadset)** first (~3.3h, fits a raised timeout) for a clean recognizable demo; build a detached/chunked long-running job for the 650k `sanadset` in parallel (does NOT block demo).
- **Issues filed (wave-24/phase-8, on board 2):** **isnad-graph#1177** `embed-hadiths --exclude-collections` scope filter (impl Aisling Brennan; reviewers Jun-Seo Park + Mateo Salazar); **deploy#529** reembed-corpus.yml raise SSH command_timeout→6h + collection-scope dispatch input (impl Aisha Idrissi; reviewers Weronika Zielinska + Lucas Ferreira); **deploy#530** detached long-running sanadset embed job (queued AFTER #529 — same file `reembed-corpus.yml`, must serialize to avoid conflict).
- **Cutover sequence once #1177+#529 merge:** rebuild embed image (CLI flag bakes in) → promote embed to stg → canonical re-embed on stg `--exclude-collections sanadset` → verify-recall + cosine probe → promote embed to prod (include embed) → **kill prod orphan + TRUNCATE prod hadith_embeddings/hadiths** (canonical-only upsert won't remove the orphan's sanadset rows) → canonical re-embed prod → verify. prod Postgres user=`IsnadDbUseer`, db=`Isnad-Prod`.

**EXECUTION LOG (2026-07-07, all merged; stg embed IN FLIGHT):**
- **BOTH PRs MERGED:** deploy **#531** (`A.Idrissi/529-...`, reviewers Weronika Zielinska + Lucas Ferreira APPROVED) → `reembed-corpus.yml` now has `command_timeout 90m→5h`, job `timeout-minutes 120→330` (5h<330<360 hosted-runner cap), and `exclude_collections` dispatch input → `isnad embed-hadiths --exclude-collections <csv>`; #529 auto-closed; deploy main green. isnad-graph **#1178** (`A.Brennan/1177-...`, reviewers Jun-Seo Park + Mateo Salazar APPROVED) → `--exclude-collections`/`--collections` mutually-exclusive CSV flags, parameterized WHERE (DB-side prune, 650k sanadset never streams), default byte-for-byte unchanged; #1177 closed.
- **Embed image rebuilt+published:** isnad-graph "Publish to GHCR" success on merge sha `6b83c9f3` → **`embed:stg-6b83c9f`** (digest `1ee19b35…`, == `stg-latest`) carries the flag. (prod embed image NOT yet built — needs promote incl. embed after stg verified.)
- **stg dry-run PASSED** (run 28870394333): resolved `image=embed:stg-6b83c9f`, `isnad embed-hadiths --batch-size 256 --exclude-collections sanadset`, model=MiniLM, exit 0. No staging approval gate.
- **stg NULL-collection = 0** (Jun-Seo caveat 1 moot); **canonical target = 125,256** (non-sanadset; identical graph both boxes = 776,239). **stg TRUNCATED** (was 34,028/33,958 mixed → 0/0).
- **stg canonical embed DONE + GATE PASSED (run 28870514744):** 125,256 hadiths / 125,256 embeddings / **0 sanadset**, skipped_empty=0, reindex ivfflat lists=185, **verify-recall passed=True structural_ok=True**, ~3.15h. Cosine probe (in stg api container, SentenceTransformerEmbedder/MiniLM) topically correct: patience→Nahj-al-Balagha 0.680, prayer→Abu Dawud/Muslim 0.77, charity→Ibn Majah 0.66 — all recognizable collections, no sanadset. **stg gate fully clean.** (stg NULL-collection=0, so exclude drops nothing legit.)
- **Prod promotion DONE:** `promote.yml` run 28883773762 `images=embed source_sha=6b83c9f` (NOTE: source_sha must be **7-char short**, not full 40 — first try failed on that) → plan✓, stg-verify gate✓, prod retag gate SELF-APPROVED (env id 13777336763) → **`embed:prod-6b83c9f` minted** (+prod-latest). Embed-only ⇒ rollout auto-SKIPPED ⇒ **no prod API restart** (prod api stays on prod-3af4d39, serve-path identical).
- **Prod dry-run PASSED** (run 28883887088, self-approved): image=prod-6b83c9f (guard ok), cmd `isnad embed-hadiths --batch-size 256 --exclude-collections sanadset`, exit 0.
- **Prod orphan KILLED + prod TRUNCATED:** stopped container `noorinalabs-isnad-graph-embed-run-5042f82fe983` (was at 518,144 mixed / 485k sanadset), procs=0; TRUNCATEd prod hadith_embeddings+hadiths → 0/0.
- **REAL prod canonical embed IN FLIGHT: run 28884008455** (env=prod, exclude_collections=sanadset, dry_run=false, image prod-6b83c9f), self-approved, ~3.3h, watching (bg b8otfhcid). Expected 125,256 / 0 sanadset.
- **✅ CUTOVER COMPLETE (2026-07-07) — prod semantic search FUNCTIONAL on clean canonical corpus.** Prod real embed (run 28884008455) success, ~2.9h: **125,256/125,256/0-sanadset**, verify-recall passed (RC-gate green). Prod cosine probe (api container, MiniLM): patience→Al-Kafi 0.713/Nahj 0.680, prayer→Abu Dawud 0.772/Muslim 0.746, charity→Al-Kafi 0.654 — all recognizable collections, no sanadset. **prod↔stg EXACT parity**: both 125,256 emb / 53 collections / 0 sanadset / identical top-5 (musnad_ahmad 26,363, tahdhib 13,201, muslim 7,314, bukhari 7,238, nasai 5,680). Minor cosine ordering deltas = ivfflat approximate-index on independently-built indexes, NOT data delta.
  - **deploy#470 (prod corpus provisioning) + deploy#523 (embedder parity) fully RESOLVED in substance** — prod was empty/503 before; now clean canonical MiniLM corpus, real semantic ranking, stg-gated + IaC. Only box one-offs were the deliberate orphan-kill + TRUNCATE (documented). Prod approval env id = **13777336763**.
  - **Reference for future embeds:** promote source_sha = **7-char short**; reembed guard rejects stg-* on prod (needs prod-* image); promote images=embed only ⇒ rollout auto-skips (no api restart); prod/stg Postgres user=`IsnadDbUseer` db=`Isnad-Prod`; probe script pattern = get_embedder()/to_pgvector_literal + PgClient.execute cosine `1-(e.embedding<=>%s::vector)`.
  - **STILL OPEN / NEXT:** **deploy#530** (650k sanadset ~20h detached-poll embed job — can't run on 6h hosted runner). Then owner-sequenced **donor-readiness graph fixes**: **da#325** (chain hadith_id via `hadith_node_id()` + reload) and **da#326** (enrich centrality/community for choke-points) — user said do these AFTER embedder cutover (now done).
- **deploy#530** (sanadset ~20h detached job) still queued — cannot run on hosted runner (6h cap); needs launch-detached-and-poll design.
