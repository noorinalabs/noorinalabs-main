---
name: project_main136_pipeline_e2e_lands_in_ingest
description: "main#136 pipeline worker E2E scenarios land in ingest-platform (not deploy/not main), authored by an ingest persona"
metadata: 
  node_type: memory
  type: project
  originSessionId: 080813cd-f3b8-434d-974c-badf58620c96
---

main#136 ("integration-tests: add pipeline worker scenarios — dedup, enrich, normalize, graph-load") is a follow-up to #49's cross-repo suite, but the actual code does NOT belong in noorinalabs-main.

**Where it lands:** `noorinalabs-isnad-ingest-platform/tests/integration/` (testcontainers harness; neo4j+postgres containers, `kafka-python` available). NOT the deploy `integration-tests` compose suite — that `docker-compose.test.yml` has Neo4j only (no Kafka/MinIO/worker containers), so adding the pipeline there is a large infra lift. The #49 README (`noorinalabs-deploy/integration-tests/README.md`) names #136 as the deferred follow-up but the natural home given the existing harness is ingest-platform.

**What's missing (the actual #136 ask):** the dedup→enrich→normalize→graph-load WORKER-CHAIN E2E through Kafka topics + object-store, plus admin stage-reset (`src/pipeline/reset.py`, #108). Per-worker UNIT tests already exist (`tests/workers/`, ~70 tests) and a parse→graph-load→Neo4j flow exists (`tests/integration/test_real_data_flow.py` — direct loaders, no Kafka). No worker-chain test exists as of 2026-06-10.

**Ownership constraint:** ingest-platform tracks its own `main` (GitHub Flow) — NO `deployments/phase-4/wave-2` branch. Commits must be authored by an INGEST roster persona (e.g. Imelda Santos), per [[feedback_child_repo_implementer_rule]] + the #936 hook>brief precedent + the new commit-identity gate (main#627). A parent-team persona (Aino Virtanen) committing there violates the child-repo rule. main#105–108 (B2/Kafka/worker-containers/reset) are all CLOSED.

**Overlap:** main#139 (Nikolaos Papadopoulos) is the live one-shot E2E of the same pipeline; #136 is the repeatable test-suite layer — reuse #139's stack/fixtures.

**FINAL OUTCOME 2026-06-11 — routed to a real ingest agent (Tomás Carvalho), my PR closed.** Crossed messages: lead had ALREADY spawned **Tomás Carvalho** (ingest QA, owns the #55 harness) to own #136 before my "proceeding" note landed. Two agents on one issue = stand down. Crucially, the lead corrected my plan to author as **Petra Vidović**: an agent (Aino) committing under another persona's identity IS the persona-divergence the main#627 gate exists to catch — "the hook would technically allow it" (parent roster merges into child) is the TELL that it's the wrong pattern, not permission. LESSON: child-repo work goes to a real agent of that child's team, NOT me-wearing-their-name. I closed my redundant PR #64, deleted the branch + worktree, and handed my surface analysis to Tomás (the reusable bits: faithful-fake stack ALREADY in `tests/workers/conftest.py` = FakeS3Client/FakeProducer/FakeConsumer/ObjectStore + parquet builders; chain contract `Proc(store)(msg)->next_msg`; the genuine gap = #108 reset→rerun via `PipelineResetter`, needing a FakeS3Client subclass with `list_objects_v2`/`delete_objects`). Tomás's branch: `T.Carvalho/0136-worker-chain-scenarios`. Updates [[feedback_child_repo_implementer_rule]] with the don't-wear-their-identity nuance.

**ACTUAL FINAL 2026-06-11 — Option A, trimmed, PR #65.** More crossed messages: after the stand-down, the lead's REAL decision arrived = Option A — KEEP Petra authorship (she's the genuine domain owner of the `tests/integration`+`tests/workers` infra; legit ingest-persona attribution for an ingest PR, NOT a divergence) and TRIM to the reset-rerun gap only. Tomás reassigned to ig#63 ONLY (his 0136 branch was empty/never-pushed — no collision). Since I'd already closed PR #64 + deleted the branch, I RECREATED clean: branch `P.Vidovic/0136-reset-rerun-scenario`, file `tests/integration/test_pipeline_reset_rerun.py` (ONLY `TestAdminStageResetThenRerun`), **PR #65**. CI: lint/test/sync green, only `security-audit` red (pre-existing pip CVE = main#633, deliberately not folded in). Reviewers = ingest-roster (lead assigns). NET LESSON on identity: keep-vs-divergence depends on whether the persona genuinely owns the domain — Petra owns ingest test infra so it's legit; the earlier worry was overcautious. The hard rule stays: don't invent a cross-repo persona you have no basis for ([[feedback_child_repo_implementer_rule]], #936 Kofi-Mensah case).

**TRUE FINAL 2026-06-11 — PR #65 CLOSED, superseded by Tomás's #67. #136 is fully Tomás's.** Tomás (now owning #136 broadly + ig#63) opened **PR #67**: the COMPLETE dedup→enrich→normalize→ingest chain through REAL Kafka+MinIO+Neo4j + id-parity + the #108 reset against REAL MinIO. His real-MinIO reset xfails on a GENUINE prod bug: `reset._delete_prefix` → bulk `delete_objects` → `MissingContentMD5` on real S3/MinIO. **My #65 reset-rerun PASSED only because `FakeS3Client.delete_objects` is a no-op — the exact mock that HID that bug.** So #65 was a mock-masked green; #67 is the honest version. Lead reversed his earlier keep-#65 call (owned the crossed-message thrash); I closed #65 ("superseded by #67"), deleted branch+worktree. THE LESSON (high-value, reusable): a green in-process test whose fake no-ops the real failure path gives FALSE CONFIDENCE — when a real-infra test xfails on a bug your mock can't see, the real-infra test is authoritative and the mocked one should be closed/relabeled, not merged alongside. Cf. [[feedback_test_mock_masks_prod_failure]]. My #136 contribution = the gap audit + surface analysis that fed Tomás's #67; I'm clear of #136.

**Blocked-but-not-mine:** ingest CI `security-audit` (`pip-audit --strict`) fails on `pip 26.1.1` → PYSEC-2026-196 (fix 26.1.2), a transitive CVE via `pip-audit>=2.7` — newly disclosed, green on main's last run, fails EVERY ingest PR (incl. Nikolaos's #139 PR #62). Test-only PRs shouldn't fold a dep bump; needs a separate pip≥26.1.2 uv.lock bump. Flagged to lead 2026-06-11.

**DUPLICATION FOUND 2026-06-11 — PR #64 mostly redundant.** The real-container worker E2E ALREADY existed on ingest `main` at my branch point (e0d127a), merged via ingest #55: `tests/integration/test_kafka_worker_e2e.py` drives enrich→normalize→ingest through REAL Kafka topics + MinIO + Neo4j testcontainers via production WorkerRunner/build_runner (`@pytest.mark.integration`, importorskip-guarded). And `test_e2e_inprocess.py` (#139) already does the in-process full chain via `scripts/e2e_pipeline_run.py` (InMemoryS3/InMemoryBroker/CapturingNeo4jDriver/build_sunnah_fixture/run_pipeline — REUSE these for any future ingest-pipeline test). So of PR #64: the real-Neo4j tail is SUBSUMED, the in-process chain OVERLAPS, and only the **#108 admin stage-reset→rerun-chain workflow is genuinely novel** (test_reset.py has 0 chain-rerun refs; nothing else combines `PipelineResetter` + a worker-chain rerun — the "#136 reset→reruns succeed" bullet). LESSON: my scope audit grepped dedup→enrich adjacency and missed the enrich→normalize→ingest file — when auditing "does test X exist", read EVERY `tests/integration/test_*{e2e,chain,pipeline}*` file by name, don't rely on a stage-pair grep. Recommended to lead: trim PR #64 to the reset-rerun scenario only; holding for A/B/C decision. Cf. [[feedback_sibling_issue_overlap]], [[feedback_pre_spawn_verify_at_origin]].

Escalated to team-lead 2026-06-10 for routing. Related: [[feedback_scope_audit_child_repo_rule]], [[feedback_artifact_gate_non_blocking]].
