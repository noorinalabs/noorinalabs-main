---
name: project_semantic_search_hashing_embedder
description: "Semantic search returns lexically-arbitrary results because the corpus is embedded with the dependency-free HashingEmbedder (token-overlap, not meaning); query-embed path is already correct (ig#1049). Real fix = real model on cluster, P5W4 (ig#1071/#1057)."
metadata: 
  node_type: memory
  type: project
  originSessionId: b923c0f4-c87a-4bed-b4b8-91a79287509b
---

isnad-graph `/search/semantic`: the query-embedding path is **correct** since **ig#1049** (closed 2026-06-14) — `search_semantic` embeds the query at request time via `to_pgvector_literal(get_embedder().embed([q])[0])` and cosine-ranks pgvector (`1 - (e.embedding <=> %s::vector) AS score`). Do NOT re-file "never embeds the query / `WHERE text = %s` exact-text lookup" — that was ig#1067, a phantom I filed off a **stale read**, closed 2026-06-14. See [[feedback_verify_diagnosis_before_delegating]].

The real reason "search for 'dog' returns unrelated hadiths": the 34,028-hadith corpus is embedded with the default `HashingEmbedder` (`EMBEDDING_MODEL=hashing`, `src/config.py:188`). Per its own docstring it ranks by **token overlap, not meaning** — meaningless-by-construction for non-overlapping queries. It exists so CI/sandbox need no `torch`.

Real fix = cluster ops (NOT a code PR — code already supports it via `EMBEDDING_MODEL`): set a multilingual 384-dim sentence-transformers model, re-embed all 34k hadiths (`isnad embed-hadiths`), `REINDEX` ivfflat. Earmarked **P5W4**. Tracked: **ig#1071** (real-model load) + **ig#1057** (reindex after bulk load). Owner 2026-06-14 chose "track only, no interim band-aid."

**OWNER DECISION 2026-06-15 (P5W4):** Do NOT run the re-embed live via one-off SSH. Instead **design a repeatable GitHub Action / IaC mechanism to trigger re-embeddings** (design-thinking pass FIRST, before any live run). Also **declined the interim min-similarity floor PR** — the real fix supersedes; don't ship the band-aid. So #1071/#1057 stay OPEN pending the re-embed-mechanism design; the wave does NOT wrap until owner steers the design. **Grounding facts** (Explore 2026-06-15): embed CLI = `isnad embed-hadiths --batch-size 256 --limit N` (`src/cli.py`), source = Neo4j `MATCH (h:Hadith)`, sink = pgvector `isnad_graph.hadith_embeddings`, dim **fixed 384** (`embeddings.py:76`) so the model MUST be 384-dim — **`paraphrase-multilingual-MiniLM-L12-v2` (384) ✓; `distiluse-base-multilingual-cased-v2` is 512-dim ✗** (would fail the dim-guard). ivfflat `WITH (lists=100)` created `IF NOT EXISTS` only — **no REINDEX command exists yet** (that's #1057's gap). `sentence-transformers`/`torch` not yet declared as a pyproject `ml` extra. Cluster = single Hetzner VPS on **docker-compose (NOT k8s)**; CI reaches it by **SSH (appleboy/ssh-action, `secrets.DEPLOY_SSH_PRIVATE_KEY`, user `deploy`) → `docker run --rm` in the compose network** — canonical precedent is `noorinalabs-deploy/.github/workflows/db-migrate.yml` (one-shot alembic on the box + `.prom` textfile-collector metrics; `workflow_dispatch` env-choice input). One-shot compose-service precedent = `user-service-migrate` (`restart: no` + `service_completed_successfully`).

**RESOLVED on STAGING 2026-06-16 (P5W4 capstone, deploy run 27583669058):** real re-embed via the mechanism PASSED — `paraphrase-multilingual-MiniLM-L12-v2`, **33,958/34,028** hadiths embedded (70 empty skipped), dim=384, reindex `lists=185`, `verify-recall` PASS (`patience` top=0.497 kw✓, `prayer` top=0.767 kw✓), `corpus_reembed.prom success=1 duration=2853s`. Wall-clock **47.5 min** (close to the 60m SSH timeout → consider 90m for prod). **Two latent gaps the capstone caught + fixed first:** (1) embed image `isnad` CLI `ModuleNotFoundError: No module named 'src'` — the console-script (`src.cli:main`) couldn't import the `src/`-layout package because `embed-builder` runs `uv sync` with only pyproject+lock (source copied later), so the project pkg is never installed; the **api/runtime image only survived the identical gap because uvicorn inserts cwd onto sys.path** — fixed `PYTHONPATH=/app` (ig#1093/PR#1094, also applied to runtime stage). (2) real run killed by `appleboy/ssh-action` default 10m `command_timeout` — bumped to 60m + job `timeout-minutes:120` + embed mem 2G→4G (deploy#465/PR#466). **ig#1071 CLOSED 2026-06-16 as delivered-on-staging.** **PROD leg deferred — prod is EMPTY:** prod re-embed (env=prod, image sha-a367ce9) failed in 1s at `compose pull` (`no such service: isnad-graph-embed` — prod's `/opt/noorinalabs-deploy` compose predates the embed service; prod never `deploy-prod`'d since deploy#461/#462). SSH'd `deploy@noorinalabs-prod` (178.156.214.225) → prod Neo4j (`noorinalabs-neo4j-1`) has **0 nodes** — prod has NEVER been data-loaded. So prod semantic search is moot until the full prod data-pipeline cutover (load corpus → deploy-prod compose roll → re-embed); tracked **deploy#470**. deploy-prod is a full stack cutover (rolls api+frontend tags too), production-env owner-approval-gated (reviewer=parametrization), so don't roll it just for the embed. Prod env wiring that DOES work: VPS_HOST=178.156.214.225, prod `.env` present, SSH ok, image guard refuses `:stg-*` for prod (pin `sha-<sha>`). Non-blocking carry-forwards: ig#1095 (install pkg properly vs PYTHONPATH), deploy#467 (Prometheus staleness alert on `corpus_reembed_last_run_*`; a hard timeout-kill aborts before the .prom write → no metric). Staging data lacks `topic_tags`/`grade_composite` props (benign embed warnings).

Related already-fixed: relevance %>100% was raw unbounded Lucene scores → ig#1065/PR#1069 max-normalizes API-side + frontend clamp; saturating-transform refinement = ig#1070.
